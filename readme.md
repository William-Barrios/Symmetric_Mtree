Instrucciones para compilar:

    dentro de carpeta project:
        mkdir build
        cd build
        camke ..
        make 

Ejecución:
    
    dentro de build:
        ./MTree /ruta/al/archivo.json <entero cantidad maxima de nodos hijo>
        Ejemplo:
            ./MTree /Users/williambarrios/Downloads/diabete_missing_values.json 5

Para ejemplos de query y sintaxis revisar queries.txt